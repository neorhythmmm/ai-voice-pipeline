name: CI
on: [push]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: "3.12"}
      - run: pip install flake8
      - run: flake8 . --count --select=E,W,F --show-source --statistics
      - run: echo "Syntax OK"
